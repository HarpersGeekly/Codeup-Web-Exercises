/**
 * Created by RyanHarper on 11/9/16.
 */

"use strict";

var listofPeople = ['Tyler','Ryan', 'Brad', 'Gideon'];

var listofNumbers = [2, 4, 5, 6, 7, 8];

var listofDays = ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday'];


// TODO: Create an array of 4 people's names using literal array notation, in a variable called 'names'.

var names = ['Lauren' , 'Lynne' , 'Ryan', 'Grant'];

// TODO: Create a log statement that will log the number of elements in the names array.

console.log(names.length);

// TODO: Create log statements that will print each of the names array elements individually.

console.log(names[0]);
console.log(names[1]);
console.log(names[2]);
console.log(names[3]);


var namesS = [Joe, Bob, Sally];


